# ORG_NAME

Project name TBD. A hobby platform for live telemetry logging and replay for the EA Sports F1 games.

## Repos

- `platform` — Rust backend services (Cargo workspace)
- `webapp` — TypeScript frontend
- `infra` — Terraform infrastructure
- `gitops` — Argo CD-reconciled deployment manifests
- telemetry parser crate — Rust client binary that decodes and forwards telemetry packets
